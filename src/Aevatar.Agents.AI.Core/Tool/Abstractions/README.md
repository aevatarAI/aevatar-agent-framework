# Tool.Abstractions

工具系统核心抽象与基础契约定义。

## 主要内容
- `AevatarToolAttribute`：工具标记属性。
- `IAevatarTool` / `IAevatarToolManager`：工具与管理器接口。
- `ToolDefinition` / `ToolContext` / `ToolExecutionContext`：工具定义与运行上下文。
- `ToolCategory` / `RetryPolicyDefaults`：工具分类与默认重试策略。

