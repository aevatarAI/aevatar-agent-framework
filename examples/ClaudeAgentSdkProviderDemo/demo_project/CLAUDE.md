# Claude Agent SDK Demo Project Instructions
#
# 目的：
# - 用于 real mode 验证 `settingSources: ['project']` 是否生效。
# - 只要 Claude Agent SDK 正确加载了本文件，输出就会出现可见标记。
#
# 规则：
# - 你的最终回答必须以如下第一行开头（大小写与标点必须完全一致）：
#   CLAUDE_MD_LOADED
# - 然后输出一个空行，再输出正常回答。
# - 回答保持简短、确定性（不要随机、不要啰嗦）。
# - 除非用户明确要求，否则不要解释本文件的存在或内容。


