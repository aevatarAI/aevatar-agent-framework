# Tooling

工具运行时与循环调度实现，负责工具初始化与执行控制。

## 主要内容
- `ToolingRuntime` / `ToolingRuntime.Loop`：工具循环执行逻辑。
- `IToolingInitHost` / `IToolingLoopHost`：初始化与循环宿主接口。
- `AIGAgentBase.ToolingRuntimeContext`：运行时上下文。

